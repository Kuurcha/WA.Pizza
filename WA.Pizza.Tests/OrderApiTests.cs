using System;
using Wa.Pizza.Infrasctructure.Services;
using Xunit;

namespace WA.Pizza.Tests
{
	[Collection("Test database collection")]
	public class OrderApiTest
	{
/*		private readonly DatabaseCollection _fixture;
		private readonly OrderDataService _orderDataService;
		public OrderApiTest()
        {
			_fixture = new DatabaseCollection();
			_orderDataService = new OrderDataService(_fixture.applicationDbContext);
			ApplicationUser applicationUser = new ApplicationUser { };
			applicationUser = _fixture.applicationDbContext.ApplicationUser.Add(applicationUser).Entity;
			_fixture.applicationDbContext.SaveChanges();
			Basket basket = new Basket() { ApplicationUserId = applicationUser.Id, LastModified = DateTime.Now };
			
        }

		[Fact]
		public void order_is_added()
        {

        }*/
	}
}
